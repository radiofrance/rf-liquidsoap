let formats.stereo.hls.fdkaac = [
 ("lofi",
  %ffmpeg(
    format="mpegts",
    %audio(
      codec="libfdk_aac",
      channels=2,
      b="32k",
      samplerate=48000,
      afterburner=1,
      profile="aac_low"
    ))
 ),
 ("midfi",
  %ffmpeg(
    format="mpegts",
    %audio(
      codec="libfdk_aac",
      channels=2,
      b="96k",
      samplerate=48000,
      afterburner=1,
      profile="aac_low"
    ))
 ),
 ("hifi",
  %ffmpeg(
    format="mpegts",
    %audio(
      codec="libfdk_aac",
      channels=2,
      b="192k",
      samplerate=48000,
      afterburner=1,
    profile="aac_low"
    ))
 )
]

let formats.surround.hls.fdkaac = [
 ("hifi",
  %ffmpeg(
    format="mpegts",
    %audio(
      codec="libfdk_aac",
      channel_layout=5.1,
      b="256k",
      samplerate=48000,
      afterburner=1,
    profile="aac_low"
    ))
 ),
 ("insane",
  %ffmpeg(
    format="mpegts",
    %audio(
      codec="libfdk_aac",
      channel_layout=5.1,
      b="320k",
      samplerate=48000,
      afterburner=1,
    profile="aac_low"
    ))
 )
]
