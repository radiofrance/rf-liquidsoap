liquidsoap
icecast
nginx
apache
mopidy

Linode Debian 12 2GB Shared

login as root
apt update
apt upgrade
apt install make docker curl git

adduser chuntfm
usermod -a -G sudo chuntfm

# install docker
curl -sSL https://get.docker.com/ | sudo sh
# add chuntfm to docker group
sudo usermod -aG docker chuntfm

su chuntfm
cd
git clone https://github.com/chuntfm/rf-liquidsoap.git
cd rf-liquidsoap


make start
 # get hit by tons of messages

# start editing the docker makesfiles and liquidsoap configs